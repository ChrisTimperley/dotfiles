- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
    ~/.tmuxconf: tmux/tmux.conf
    ~/.gitconfig: git/gitconfig

- shell:
    - ["vim +PluginInstall +qall", "Installing vim plugins"]
